class UsageEntity {

  String ? fechaRegistro;
  String ? tiempoUso;
  String ? accesos;
  String ? nivelAdiccion;
  String ? appName;
  String ? momentoDia;

  UsageEntity({
    this.fechaRegistro,
    this.tiempoUso,
    this.accesos,
    this.nivelAdiccion,
    this.appName,
    this.momentoDia,
  });
}